#ifndef CALEVENTS
#define CALEVENTS 1
#include <string>

struct Cal_events{
    std::string summary;
    std::string date_start;
    std::string date_end;
    std::string location;
};

#endif // CALEVENTS
